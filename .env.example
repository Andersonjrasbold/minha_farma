FLASK_ENV=development
SECRET_KEY=Farmaoferta2@26

# Opção local (SQLite):
DATABASE_URL=sqlite:///minha_farma.db

# Opção Supabase (PostgreSQL, Pooler recomendado):
# SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://postgres.<REF>:SUA_SENHA@aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require

# Opção direta (se disponível):
# SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://postgres:SUA_SENHA@db.<REF>.supabase.co:5432/postgres?sslmode=require

# SQLite fallback explícito (defina como true apenas se quiser usar local):
# SQLITE_FALLBACK_ENABLED=false

DEFAULT_CITY=Cascavel
